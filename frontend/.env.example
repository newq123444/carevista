# Leave this UNSET for local dev — the Vite dev server proxies /api to the backend.
# Only set it for a deployed frontend, and include the /api path:
# VITE_API_URL=https://your-backend.onrender.com/api
